import urllib.request
from colorama import Fore
from time import sleep
while(1):
	sleep(1)
	def connect():
		try:
			urllib.request.urlopen('http://google.com') 
			return True
		except:
			return False
	print("وصل نیست")
	if connect() == True:
		break

#کد زیر پس از وصل شدن اینترنت اجرا خواهد شد و هرگز زمانی که اینترنت وصل نیست پردازش نخواهد شد 

#code: 

print("وصل شد ")
